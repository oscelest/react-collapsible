enum CollapsibleDirection {
  WIDTH             = "width",
  HEIGHT            = "height",
  BOTH              = "both",
  BOTH_WIDTH_FIRST  = "wboth",
  BOTH_HEIGHT_FIRST = "hboth",
}

export default CollapsibleDirection;
